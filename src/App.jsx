// TEMPORARY dev entry point — exists only to mount the hook test harness.
// Delete this file (and src/dev/) once Jennifer's real components/App land.
import HookTestHarness from './dev/HookTestHarness.jsx';

export default function App() {
  return <HookTestHarness />;
}
